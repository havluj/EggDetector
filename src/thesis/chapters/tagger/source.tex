\chapter{Tagger}\label{chap:tagger}
Tato pøíloha obsahuje zdrojový kód a zpùsob použití nástroje \texttt{Tagger}. Kompletní dokumentace a zdrojový kód je dostupný na pøiloženém médiu (viz pøíloha \ref{chap:cd}).

\section{Dokumentace (v~AJ)}
Data tagger is a simple web application used for data tagging used to train a neural network. The data strictly needs to follow the structure of \url{http://athena.pef.czu.cz/ptacionline/134572snaps/}

\subsection{Usage manual}
\begin{itemize}
	\item Compile (if needed) the app with \texttt{mvn clean package}
	\item Run with \texttt{target/run.sh} on Linux or \texttt{target\textbackslash run.bat} on Windows. Both scripts accept data location as the first and only parameter.
	\item For example: \texttt{target/run.sh /home/test/egg/data/nest1}
\end{itemize}

\section{Zdrojový kód}
Zdrojový kód je dostupný na pøiloženém médiu (viz pøíloha \ref{chap:cd}).

\section{Ukázkový výstup}
Po urèení poètu vajec na každém snímku je do pøíslušné složky vygenerován soubor \texttt{imgdata.xml}, který je použit pro trénování a pozdìji testování neuronové sítì. Níže je pøiložen ukázkový výpis souboru.

\newpage
\lstinputlisting[language=XML,label={list:imgdata},caption={Ukázkový výstup programu Tagger.}]{media/imgdata.xml}